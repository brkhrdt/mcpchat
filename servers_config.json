{
    "mcpServers": {
        "filesystem": {
            "command": "docker",
            "args": [
                "run",
                "-i",
                "--rm",
                "--mount", "type=bind,src=/home/s/git/simple-chatbot-mcp/filesystem_test,dst=/projects",
                "mcp/filesystem",
                "/projects"
            ]
        }
    }
}
